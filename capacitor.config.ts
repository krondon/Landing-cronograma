import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.morna.cronograma',
  appName: 'Expo FedeIndustria',
  webDir: 'dist'
};

export default config;
